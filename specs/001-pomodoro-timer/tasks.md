# Tasks: 基本番茄鐘

**Input**: Design documents from `/specs/001-pomodoro-timer/`
**Prerequisites**: plan.md (required), spec.md (required), research.md, data-model.md, contracts/timer-api.md

**Tests**: 未在規格書中明確要求 TDD，測試任務置於 Polish 階段作為品質保障。

**Organization**: 任務按使用者故事分組，每個故事可獨立實作與測試。

## Format: `[ID] [P?] [Story] Description`

- **[P]**: 可平行執行（不同檔案，無依賴）
- **[Story]**: 所屬使用者故事（US1, US2, US3）
- 包含確切檔案路徑

---

## Phase 1: Setup

**Purpose**: 專案初始化與基礎結構建立

- [x] T001 建立專案目錄結構 src/ 與 tests/
- [x] T002 [P] 建立 src/__init__.py 與 tests/__init__.py

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: 所有使用者故事共用的基礎元件，MUST 在任何故事開始前完成

**⚠️ CRITICAL**: 使用者故事階段在此階段完成前不得啟動

- [x] T003 建立 TimerState 與 TimerMode 列舉類別在 src/timer.py
- [x] T004 [P] 建立音效播放模組 play_alert() 在 src/sound.py

**Checkpoint**: 基礎元件就緒——使用者故事實作可開始

---

## Phase 3: User Story 1 - 工作倒數計時 (Priority: P1) 🎯 MVP

**Goal**: 使用者可啟動 25 分鐘倒數計時，計時結束時播放音效提醒

**Independent Test**: 啟動應用程式 → 點擊「開始」→ 計時器從 25:00 倒數至 00:00 → 音效響起

### Implementation for User Story 1

- [x] T005 [US1] 實作 Timer 類別的建構子、start()、tick()、formatted_time() 方法在 src/timer.py
- [x] T006 [US1] 建立 Tkinter 主視窗，包含大型 MM:SS 倒數計時器顯示在 src/app.py
- [x] T007 [US1] 加入「開始」按鈕並綁定 Timer.start() 在 src/app.py
- [x] T008 [US1] 使用 Tkinter after() 實作每秒 tick 回呼迴圈，搭配 time.monotonic() 校正誤差在 src/app.py
- [x] T009 [US1] 整合計時結束時觸發 sound.play_alert() 並顯示「工作完成」在 src/app.py
- [x] T010 [US1] 建立應用程式入口點，組裝 Timer 與 App 並啟動 mainloop 在 src/main.py

**Checkpoint**: 此時 User Story 1 應完全可用——啟動程式 → 開始倒數 → 25:00→00:00 → 音效提醒

---

## Phase 4: User Story 2 - 暫停與控制 (Priority: P2)

**Goal**: 使用者可暫停、繼續、重置計時器

**Independent Test**: 開始倒數 → 暫停（計時器停止）→ 繼續（從暫停處恢復）→ 重置（回到 25:00）

### Implementation for User Story 2

- [x] T011 [US2] 實作 Timer.pause() 與 Timer.reset() 方法在 src/timer.py
- [x] T012 [US2] 加入「暫停/繼續」切換按鈕，根據 Timer.state 動態切換按鈕文字在 src/app.py
- [x] T013 [US2] 加入「重置」按鈕，綁定 Timer.reset() 並更新 UI 狀態在 src/app.py
- [x] T014 [US2] 處理按鈕狀態連動：開始後啟用暫停/重置、暫停後顯示繼續、重置後顯示開始在 src/app.py

**Checkpoint**: User Story 1 + 2 均可獨立運作——完整的 開始/暫停/繼續/重置 流程

---

## Phase 5: User Story 3 - 工作與休息循環 (Priority: P3)

**Goal**: 工作結束後自動切換至 5 分鐘休息，休息結束後回到工作模式

**Independent Test**: 工作計時結束 → 切換至休息 05:00 → 休息結束 → 回到工作 25:00

### Implementation for User Story 3

- [x] T015 [US3] 實作 Timer.switch_mode() 方法在 src/timer.py
- [x] T016 [US3] 加入模式顯示標籤（「工作中」/「休息中」），根據 Timer.mode 更新在 src/app.py
- [x] T017 [US3] 實作計時結束後的模式切換邏輯：工作→提示進入休息→休息倒數→提示回到工作在 src/app.py

**Checkpoint**: 所有使用者故事均可獨立運作——完整的番茄鐘工作/休息循環

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: 邊界案例處理、測試、品質驗證

- [x] T018 [P] 撰寫 Timer 類別單元測試（建構子、start、pause、reset、tick、switch_mode、formatted_time）在 tests/test_timer.py
- [x] T019 處理邊界案例：00:00 瞬間暫停仍觸發提醒、快速連點按鈕的狀態一致性在 src/app.py
- [x] T020 執行 quickstart.md 驗證流程，確認所有操作步驟可重現

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: 無依賴——可立即開始
- **Foundational (Phase 2)**: 依賴 Setup 完成——阻擋所有使用者故事
- **User Story 1 (Phase 3)**: 依賴 Foundational 完成
- **User Story 2 (Phase 4)**: 依賴 User Story 1 完成（在 Timer 與 App 上增量開發）
- **User Story 3 (Phase 5)**: 依賴 User Story 2 完成（在 Timer 與 App 上增量開發）
- **Polish (Phase 6)**: 依賴所有使用者故事完成

### Within Each User Story

- Timer 邏輯先於 UI 實作
- UI 元件先於事件整合
- 核心實作先於入口點組裝

### Parallel Opportunities

- T001 與 T002 無依賴可平行
- T003 與 T004 操作不同檔案可平行
- T018（測試）可與 T019（邊界案例）平行

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. 完成 Phase 1: Setup
2. 完成 Phase 2: Foundational（阻擋所有故事）
3. 完成 Phase 3: User Story 1
4. **驗證**：啟動程式 → 開始 → 倒數 → 音效 ✅
5. 可立即展示/使用

### Incremental Delivery

1. Setup + Foundational → 基礎就緒
2. User Story 1 → 可使用的 MVP（倒數 + 音效）
3. User Story 2 → 加入暫停/重置控制
4. User Story 3 → 完整的工作/休息循環
5. Polish → 邊界案例 + 測試 + 驗證

---

## Notes

- [P] 任務 = 不同檔案、無依賴
- [Story] 標籤對應 spec.md 中的使用者故事
- 每個故事完成後可獨立驗證
- 完成每個任務或邏輯群組後提交 commit
- 在任何 Checkpoint 停下來驗證故事獨立性
